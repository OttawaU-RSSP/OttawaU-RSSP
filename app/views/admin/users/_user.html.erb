<div>
  <h2><%= user.name %> - <%= user.type %></h2>
  <% if user.approved? %>
    Approved
  <% else %>
    <%= link_to 'Approve', approve_admin_user_path(user), confirm: 'Are you sure?' %>
  <% end %>
</div>

<%= render partial: "shared/lawyer", locals: { lawyer: @lawyer } %>

<% unless @lawyer.approved? %>
  <%= link_to "Approve", approve_admin_lawyer_path, method: :put, params: { id: @lawyer.id } %>
<% end %>
